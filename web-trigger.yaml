- name: web-auto-trigger
  description: Auto-triggers for web interface changes
  filename: cloudbuild-web.yaml
  github:
    owner: fkcurrie
    name: zntic-train
    push:
      branch: ^main$
  includedFiles:
  - web/**
  - nginx.conf
